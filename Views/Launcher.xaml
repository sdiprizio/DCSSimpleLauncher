<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DCSSimpleLauncher.Views.Launcher"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DCSSimpleLauncher.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical">
        <TextBlock x:Name="LauncherTitle" Text="Launcher" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,20"/>

        <StackPanel Orientation="Vertical" Margin="0,0,0,20">
            <ToggleSwitch OffContent="VR Off" OnContent="VR On" IsOn="{x:Bind IsVRModeOn, Mode=TwoWay}" />
            <ToggleSwitch OffContent="Launcher Off" OnContent="Launcher On" IsOn="{x:Bind IsLauncherOn, Mode=TwoWay}" />
            <Button HorizontalAlignment="Stretch" Height="60" x:Name="LaunchDCS" Content="Launch DCS" Click="LaunchDCS_Click" Margin="0,20,0,0"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
            <Button x:Name="ForceStopDCS" Content="Force Stop DCS" Click="ForceStopDCS_Click" />
            <TextBlock Text="This will kill all DCS instances. Use it wisely!" Padding="20,5,0,0" />
        </StackPanel>
    </StackPanel>
</Page>
